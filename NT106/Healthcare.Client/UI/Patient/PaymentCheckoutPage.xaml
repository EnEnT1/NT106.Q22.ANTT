<Page
    x:Class="Healthcare.Client.UI.Patient.PaymentCheckoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F8FAFC">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Lịch Sử Giao Dịch" FontSize="28" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="Xem tất cả các khoản thanh toán trực tuyến và tại quầy của bạn" Foreground="#64748B" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20">
            <ListView x:Name="TransactionsListView" SelectionMode="Single" SelectionChanged="TransactionsListView_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TransactionRef}" FontWeight="Bold" FontSize="16" Foreground="#1E293B"/>
                                    <TextBlock Text="{Binding AppointmentId}" FontSize="12" Foreground="#64748B"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding AmountFormatted}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#0284C7"/>
                                <Border Grid.Column="2" Background="{Binding StatusBackgroundColor}" CornerRadius="4" Padding="8,4" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusTextColor}" FontSize="12" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Popup Detail -->
        <ContentDialog x:Name="TransactionDetailDialog"
                       CloseButtonText="Đóng"
                       CornerRadius="16">
            <StackPanel MaxWidth="400" Spacing="24">
                <TextBlock x:Name="DetailTitle" Text="Chi tiết giao dịch" FontSize="20" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                
                <Border Background="#F1F5F9" CornerRadius="12" Padding="20">
                    <StackPanel Spacing="8">
                        <Grid>
                            <TextBlock Text="Số tiền:" Foreground="#64748B"/>
                            <TextBlock x:Name="DetailAmount" Text="---" FontWeight="Bold" Foreground="#0284C7" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Mã giao dịch:" Foreground="#64748B"/>
                            <TextBlock x:Name="DetailRef" Text="---" Foreground="#1E293B" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Trạng thái:" Foreground="#64748B"/>
                            <TextBlock x:Name="DetailStatus" Text="---" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>

<Page
    x:Class="Healthcare.Client.UI.Patient.PaymentCheckoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackdropGradientBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Lịch Sử Giao Dịch" FontSize="28" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="Xem tất cả các khoản thanh toán trực tuyến và tại quầy của bạn" FontSize="15" Foreground="#64748B" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20">
            <ListView x:Name="TransactionsListView" SelectionMode="Single" SelectionChanged="TransactionsListView_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TransactionRef}" FontWeight="Bold" FontSize="16" Foreground="#1E293B"/>
                                    <TextBlock Text="{Binding AppointmentId}" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding AmountFormatted}" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0" FontWeight="Bold" Foreground="#0284C7"/>
                                <Border Grid.Column="2" Background="{Binding StatusBackgroundColor}" CornerRadius="6" Padding="12,6" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusTextColor}" FontSize="13" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Popup Detail -->
        <ContentDialog x:Name="TransactionDetailDialog"
                       CloseButtonText="Đóng"
                       CornerRadius="16">
            <StackPanel MaxWidth="400" Spacing="24">
                <TextBlock x:Name="DetailTitle" Text="Chi tiết giao dịch" FontSize="20" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                
                <Border Background="#F1F5F9" CornerRadius="12" Padding="20">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="Số tiền:" FontSize="15" Foreground="#64748B" VerticalAlignment="Center"/>
                            <TextBlock x:Name="DetailAmount" Text="---" FontSize="16" FontWeight="Bold" Foreground="#0284C7" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Mã giao dịch:" FontSize="15" Foreground="#64748B" VerticalAlignment="Center"/>
                            <TextBlock x:Name="DetailRef" Text="---" FontSize="15" FontWeight="SemiBold" Foreground="#1E293B" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Trạng thái:" FontSize="15" Foreground="#64748B" VerticalAlignment="Center"/>
                            <TextBlock x:Name="DetailStatus" Text="---" FontSize="15" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>

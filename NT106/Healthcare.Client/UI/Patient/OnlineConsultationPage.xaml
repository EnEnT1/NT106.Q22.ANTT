<Page
    x:Class="Healthcare.Client.UI.Patient.OnlineConsultationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackdropGradientBrush}">

    <Page.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="#1D4ED8"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Scale" Value="0.98"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="#94A3B8"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,24,40,40" Spacing="32">
            
            <!-- Header -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="8">
                    <TextBlock Text="Khám Trực Tuyến" FontSize="32" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Theo dõi và tham gia các buổi tư vấn y tế từ xa của bạn" FontSize="16" Foreground="#64748B"/>
                </StackPanel>
                <Button Grid.Column="1" 
                        Content="Test Video Call UI" 
                        Click="BtnTestUI_Click" 
                        Background="#F1F5F9" 
                        Foreground="#475569" 
                        Padding="16,8" 
                        CornerRadius="8" 
                        VerticalAlignment="Top">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#E2E8F0"/>
                    </Button.Resources>
                </Button>
            </Grid>

            <!-- Upcoming Section -->
            <StackPanel Spacing="16">
                <TextBlock Text="Lịch khám sắp diễn ra" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
                
                <Grid x:Name="UpcomingEmptyState" Visibility="Collapsed" Padding="40" Background="#F8FAFC" CornerRadius="16">
                    <StackPanel Spacing="12" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE787;" FontSize="48" Foreground="#CBD5E1"/>
                        <TextBlock Text="Không có lịch khám nào sắp tới" Foreground="#64748B" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <ListView x:Name="UpcomingAppointmentsList" SelectionMode="None" ItemsSource="{x:Bind UpcomingAppointments, Mode=OneWay}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,16"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="20" Padding="24" Shadow="{StaticResource HealthcareThemeShadow}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Doctor Icon -->
                                    <Border Grid.Column="0" Width="64" Height="64" CornerRadius="32" Background="#EFF6FF">
                                        <FontIcon Glyph="&#xE77B;" Foreground="#2563EB" FontSize="28"/>
                                    </Border>

                                    <!-- Details -->
                                    <StackPanel Grid.Column="1" Margin="20,0" VerticalAlignment="Center" Spacing="4">
                                        <TextBlock Text="{Binding DoctorName}" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <FontIcon Glyph="&#xE787;" FontSize="14" Foreground="#64748B"/>
                                                <TextBlock Text="{Binding AppointmentDate}" FontSize="14" Foreground="#64748B"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <FontIcon Glyph="&#xE916;" FontSize="14" Foreground="#64748B"/>
                                                <TextBlock Text="{Binding AppointmentTime}" FontSize="14" Foreground="#64748B"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <Border HorizontalAlignment="Left" Background="{Binding BadgeBg}" CornerRadius="8" Padding="8,2" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding BadgeText}" FontSize="12" FontWeight="Bold" Foreground="{Binding BadgeForeground}"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- Action -->
                                    <Button Grid.Column="2" 
                                            Content="Tham gia khám" 
                                            Style="{StaticResource ActionButtonStyle}"
                                            Click="BtnJoin_Click"
                                            Tag="{Binding Id}"
                                            IsEnabled="{Binding IsJoinable}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- History Section -->
            <StackPanel Spacing="16">
                <TextBlock Text="Lịch sử khám trực tuyến" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
                
                <Border Background="White" CornerRadius="20" Shadow="{StaticResource HealthcareThemeShadow}">
                    <ListView x:Name="HistoryAppointmentsList" SelectionMode="None" ItemsSource="{x:Bind HistoryAppointments, Mode=OneWay}">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <Grid Padding="24,16" BorderBrush="#F1F5F9" BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Bác sĩ" FontSize="13" FontWeight="Bold" Foreground="#64748B"/>
                                    <TextBlock Grid.Column="1" Text="Ngày giờ" FontSize="13" FontWeight="Bold" Foreground="#64748B"/>
                                    <TextBlock Grid.Column="2" Text="Trạng thái" FontSize="13" FontWeight="Bold" Foreground="#64748B"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="24,16" Background="Transparent" PointerPressed="HistoryItem_Click" Tag="{Binding Id}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DoctorName}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    <TextBlock Grid.Column="1" Text="{Binding FullDateTime}" VerticalAlignment="Center" Foreground="#64748B"/>
                                    <Border Grid.Column="2" HorizontalAlignment="Left" Padding="10,4" CornerRadius="12" Background="#F1F5F9">
                                        <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="SemiBold" Foreground="#475569"/>
                                    </Border>
                                    <FontIcon Grid.Column="3" Glyph="&#xE76C;" FontSize="12" Foreground="#CBD5E1" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>

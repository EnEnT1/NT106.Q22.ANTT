<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Healthcare.Client.UI.Shell.PatientShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Healthcare.Client.UI.Shell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header Bar -->
        <Border Grid.Row="0"
                Background="#F3F3F3"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1"
                Padding="24,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="Hệ Thống Y Tế Avalon" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"
                           Foreground="#0059BB"/>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="12"
                            VerticalAlignment="Center">
                    
                    <Button x:Name="BtnNotification"
                            Style="{StaticResource DefaultButtonStyle}"
                            Width="40" Height="40" Padding="0" CornerRadius="20">
                        <FontIcon Glyph="&#xEA8F;" FontSize="18"/>
                    </Button>

                    <Border Width="1" Height="24" Background="#E0E0E0" Margin="4,0"/>

                    <StackPanel VerticalAlignment="Center" Spacing="0">
                        <TextBlock x:Name="TxtDate" FontSize="11" Foreground="#64748B" Text="Hôm nay"/>
                        <TextBlock Text="KẾ HOẠCH KHÁM" FontSize="9" FontWeight="Bold" Foreground="#0059BB"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <NavigationView
            Grid.Row="1"
            x:Name="NavView"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            OpenPaneLength="240"
            SelectionChanged="NavView_SelectionChanged"
            Loaded="NavView_Loaded">

            <NavigationView.PaneHeader>
                <Border Padding="12,12,12,20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <PersonPicture x:Name="PatientAvatar" Width="40" Height="40"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="TxtPatientName" Text="Bệnh nhân" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Thành viên hạng Vàng" FontSize="10" Foreground="#64748B"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </NavigationView.PaneHeader>

            <NavigationView.MenuItems>
                <NavigationViewItem Tag="PatientHomePage" Content="Trang chủ">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE80F;"/></NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="BookAppointmentPage" Content="Đặt lịch khám">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE787;"/></NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="MyRecordsPage" Content="Sổ khám bệnh" IsEnabled="False">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9F9;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="NavLogout" Content="Đăng xuất" Tapped="NavLogout_Tapped">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xF3B1;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Background="{StaticResource ApplicationPageBackgroundThemeBrush}"/>
        </NavigationView>
    </Grid>
</Page>
